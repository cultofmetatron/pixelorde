var msg = (x) => { return x + ' world!'}

console.log(msg('hello'))
var React = require('react');
var Matter = require('../components/matter');
React.render(<Matter/>, document.getElementById('example'));
